akka {
  log-level = "DEBUG"

  inject.mode = "config"
  inject.modules = ["mm4s.examples.consulbot.ConsulBotModule"]
}

mm {
  host = "localhost"
  port = 8080
  bot.user = "admin"
  bot.pass = "password"
  bot.team = "bots"
  bot.channel = "Off-Topic"
}
